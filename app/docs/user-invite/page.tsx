import { PlaceholderDoc } from "@/components/docs/PlaceholderDoc";
import { copy } from "@/lib/copy";

export default function UserInvitePage() {
  return <PlaceholderDoc title={copy.nav.userInvite} />;
}
